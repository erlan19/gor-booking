{
  "name": "gor-booking",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "build": "cd backend/gor-api && npm install && npm run build",
    "start": "cd backend/gor-api && node src/index.js"
  }
}
