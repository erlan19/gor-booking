{
  "name": "gor-booking",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd backend/gor-api && npm install && npm run build",
    "start": "cd backend/gor-api && npx prisma db push --skip-generate 2>/dev/null; npx tsx src/index.ts"
  }
}